syntax = "proto3";

package proto;

service NiKLog
{
	//日志通用传送接口
	rpc SendLog(NiKLogger) returns(R) {}
}

message NiKLogger
{
	string LogType		= 1;	//日志类型
	string LogContent	= 2;	//日志内容
}

message R
{

}